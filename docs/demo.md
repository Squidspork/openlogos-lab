# Demo Mode

Demo mode lets people try the terminal UI without LM Studio.

```bash
openlogos-lab --demo
openlogos-lab chat --demo "everyone introduce yourselves"
openlogos-lab run --demo "ideas for apps around local-first AI memory"
```

Demo responses are deterministic and clearly labeled. They are not model output.
